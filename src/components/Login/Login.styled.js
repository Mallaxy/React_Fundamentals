import styled from 'styled-components';

export const StyledLoginForm = styled.form`
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 25rem;
  margin: 0 auto;
  gap: 1rem;
`;

export const LoginFormTitle = styled.h1`
  font-size: 28px;
`;

export const LoginFormRedirect = styled.div`
  display: flex;
  gap: 0.5rem;
  margin: 0 auto;
`;
